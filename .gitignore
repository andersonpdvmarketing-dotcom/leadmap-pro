node_modules/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store
*.pem
*.key
.impeccable/
.vercel
.env*
# .env.example não tem segredos: é o modelo das variáveis do backend
!.env.example

# Ferramenta local de deploy (Vercel CLI) — não é dependência da aplicação
package.json
package-lock.json
